---
applications:
- instances: 1
  timeout: 180
  name: RubberDuckHackathon
  buildpack: python_buildpack
  command: python manage.py start 0.0.0.0:$PORT
  memory: 256M
  # domain: Ducky.apps.internal
  # host: not-used
  env:
    FLASK_APP : server
    FLASK_DEBUG : false
